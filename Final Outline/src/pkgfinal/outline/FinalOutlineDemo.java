
package pkgfinal.outline;

import java.util.Scanner;

public class FinalOutlineDemo {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        // Create an array named finalOutlineArray with 7 elements of type FinalOutline
        FinalOutline[] finalOutlineArray = new FinalOutline[7];

        // Write loops to ask the user to enter each FinalOutline data
        // Use the FinalOutline class methods to set the data for each FinalOutline

        // Use selection sort algorithm to sort the finalOutlineArray based on the highest sale in descending order

        // Display the result using the FinalOutline class methods

    }
}

